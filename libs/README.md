# Libraries

The application loads PDF.js and Mammoth.js from CDN in `index.html` for GitHub Pages simplicity. To vendor dependencies, place browser builds in this directory and update the script tags in `index.html`.
